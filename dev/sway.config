# Minimal development Sway config for vibeshell.

set $mod Mod4

# Launch terminal.
bindsym $mod+Return exec foot

# Placeholder: future panel command.
bindsym $mod+p exec echo "TODO: start panel"

# Launcher toggle.
# If launcher exists, close it. Otherwise start it.
bindsym $mod+space exec swaymsg '[app_id="com.vibeshell.launcher"] kill' || launcher

# Close launcher quickly.
bindsym Escape exec swaymsg '[app_id="com.vibeshell.launcher"] kill'

# Exit nested session.
bindsym $mod+Shift+e exec swaymsg exit
